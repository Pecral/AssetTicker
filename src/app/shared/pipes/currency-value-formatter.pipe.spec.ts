/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CurrencyValueFormatterPipe } from './currency-value-formatter.pipe';

describe('Pipe: CurrencyValueFormattere', () => {
  it('create an instance', () => {
    let pipe = new CurrencyValueFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});